int main(void)
{
    int x;

    int y = 10;

    x = ~~((!(-(2 + 3 * 4) / 2 % 7 == 0) != 1) && (2 + 3 * 4 > 10) || (5 - 3 <= 1));

    ;

    int z;
    if (y)
    {
        if (y > 15)
            z = 1;
        else
        {
            z = 2;
            z = 4;
        }
    }

    int w = (z == 1) ? (z * 2) : (z * 3);

    y *= 20;

    return w;
}