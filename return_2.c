int main(void)
{
    int x;

    int y = 10;

    x = ~~((!(-(2 + 3 * 4) / 2 % 7 == 0) != 1) && (2 + 3 * 4 > 10) || (5 - 3 <= 1));

    ;

    int z;
    if (y)
    {
        if (y > 15)
            z = 1;
        else
        {
            z = 2;
            z = 4;
        }
        int y = 5;
    }

    switch (y)
    {
    case 10:
        y = 11;
    case 20:
    {
        y = 12;
        break;
    }
    default:
        y = 13;
    }

    int w = (z == 1) ? (z * 2) : (z * 3);

    while (z > 0)
    {
        z = z - 1;
    }

    for (int i = 0; i < w; i = i + 1)
    {
        z = z + 1;
    }

    do
    {
        z = z - 1;
    } while (z > 0);

    y *= 20;

    return z;
}